Assume that you are a Senior Java spring-boot developer.


Generate service with function that will save @sample-response.json as jsondb to postgres db with Id of IMDB id. Also make a function that fetches the data using imdb id.


Make it connected/integrated to a GrpcService

